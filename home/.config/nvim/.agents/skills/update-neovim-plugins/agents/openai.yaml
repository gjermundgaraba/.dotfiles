interface:
  display_name: "Update Neovim Plugins"
  short_description: "Review and safely update vim.pack plugins"
  default_prompt: "Use $update-neovim-plugins to review and update this Neovim configuration’s plugins."
policy:
  allow_implicit_invocation: false
